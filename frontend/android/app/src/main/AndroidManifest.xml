<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- image_picker uses the system Photo Picker (ACTION_PICK_IMAGES) /
         ACTION_GET_CONTENT, which does not require READ_MEDIA_IMAGES,
         READ_MEDIA_VIDEO or READ/WRITE_EXTERNAL_STORAGE — none of those
         are declared by image_picker_android's own manifest either.
         Declaring them ourselves was unnecessary and triggered Google Play's
         media-permission policy review; removed. -->

    <!-- open_filex (used only to open a PNG the app itself just exported —
         the player-card image — via OpenFilex.open in
         player_card_export_service.dart) unconditionally declares broad
         media-read permissions in its own manifest. We never need
         library-wide media access, so strip them from the merged manifest;
         this is what Google Play flagged (READ_MEDIA_IMAGES/READ_MEDIA_VIDEO). -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />

    <!-- Package visibility (Android 11+) — required so url_launcher can open
         WhatsApp / browser / dialer via canLaunchUrl & launchUrl -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <!-- Direct whatsapp:// scheme — covers WhatsApp & WhatsApp Business -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="whatsapp" />
        </intent>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
    </queries>

    <application
        android:label="nosait academy"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Google AdMob App ID. الـ SDK يقرؤه من هنا؛ غيابه يُسقط التطبيق
             عند التهيئة. وحدات الإعلانات نفسها في lib/core/ads/ad_ids.dart. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-6344883554405987~3739372322" />

    </application>
</manifest>
